[project]
name = "templateer"
version = "0.3.0"
description = "Typed, constrained artifact generation for AI agents"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "pydantic-ai-slim[openai]>=2,<3",
    "minijinja>=2.0",
    "pyyaml>=6.0",
    "click>=8.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ty==0.0.40",
    "ruff>=0.3.0",
]

[project.scripts]
templateer = "templateer.cli:entrypoint"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/templateer"]

[tool.hatch.build.targets.sdist]
include = [
    "src",
    "README.md",
    "tests",
    "pyproject.toml",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.pytest.ini_options]
testpaths = ["tests", "templates"]

[tool.ty]
# ty uses pyproject.toml project metadata (requires-python, name) by default.
